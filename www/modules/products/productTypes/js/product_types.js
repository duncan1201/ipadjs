app.controller('productTypeCtrl',
               function($rootScope, $scope){
                    
               });

var productTypes = angular.module('productTypes', ['ionic', 'util']);

productTypes.factory('Products',
                     function(DbUtil) {
                        
                     });